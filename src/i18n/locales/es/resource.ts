const login = require("./login.json");
const animal = require("./animal.json");
const rescues = require("./rescues.json");
const notFound = require("./notFound.json");
const newAnimal = require("./newAnimal.json");

const resource = {
  login,
  animal,
  rescues,
  notFound,
  newAnimal,
};

export default resource;
